import './game.scss';
import { MaxWidth } from '../../components/MaxWidth/MaxWidth';

export const Game = () => {
  return (
    <main className="game">
      <MaxWidth>
      </MaxWidth>
    </main>
  );
}
